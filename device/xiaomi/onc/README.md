# Minimal device tree skeleton for Redmi Y3 `onc`

This directory contains a basic Android build configuration for a Redmi Y3 (`onc`) ROM project.

The file content here is an initial scaffold and will need matching to the actual stock firmware and vendor settings of the device.
